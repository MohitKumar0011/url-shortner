import React from 'react';

const Button = () => {
  return (
    <button className="bg-blue-600 rounded-full hover:bg-blue-600 text-white py-4 px-6 transform transition-all duration-300 hover:scale-110 text-lg">
      Shorten Now!
    </button>
  );
};

export default Button;
